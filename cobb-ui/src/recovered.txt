    };
  }, []);

  const handleNextTip = () => setCurrentTipIndex((prev) => (prev + 1) % forecastTips.length);
  const handlePrevTip = () => setCurrentTipIndex((prev) => (prev - 1 + forecastTips.length) % forecastTips.length);

  const handleRefreshForecastTips = async () => {
    setIsLoadingForecast(true);
    try {
      const lowStockCount = inventory.filter(i => i.CurrentStock <= 3).length;
      const res = await axios.post(`${API_BASE}/api/ai/demand-forecasts`, {
        todaySales: overviewStats.today.TotalSales,
        yesterdaySales: overviewStats.yesterday.TotalSales,\r
        deadStockCount: deadStock.length,\r
        lowStockCount: lowStockCount\r
      });\r
      if (res.data.tips && Array.isArray(res.data.tips) && res.data.tips.length > 0) {\r
        setForecastTips(res.data.tips);\r
        setCurrentTipIndex(0);\r
      }\r
    } catch (err) {\r
      console.error(\"Forecast Refresh Error:\", err);\r
      alert(`AI Forecast Refresh Failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsLoadingForecast(false);\r
    }\r
  };\r
\r
  const toggleListener = async () => {\r
    if (isTogglingListener) return;\r
    setIsTogglingListener(true);\r
    try {\r
      const endpoint = isListenerRunning ? 'stop' : 'start';\r
      await axios.post(`${API_BASE}/api/automation/${endpoint}`);\r
      const res = await axios.get(`${API_BASE}/api/automation/status`);\r
      setIsListenerRunning(res.data.isRunning);\r
      setListenerLogs(res.data.logs || []);\r
    } catch (err) {\r
      console.error(\"Toggle Listener error:\", err);\r
      alert(`Listener operation failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsTogglingListener(false);\r
    }\r
  };\r
\r
  const toggleGateway = async () => {\r
    if (isTogglingGateway) return;\r
    setIsTogglingGateway(true);\r
    try {\r
      const endpoint = isGatewayRunning ? 'stop' : 'start';\r
      await axios.post(`${API_BASE}/api/gateway/${endpoint}`);\r
      const res = await axios.get(`${API_BASE}/api/gateway/status`);\r
      setIsGatewayRunning(res.data.isRunning);\r
      setIsGatewayReady(res.data.isReady);\r
      setGatewayQr(res.data.qrCodeUrl);\r
      setGatewayLogs(res.data.logs || []);\r
    } catch (err) {\r
      console.error(\"Toggle Gateway error:\", err);\r
      alert(`Gateway operation failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsTogglingGateway(false);\r
    }\r
  };\r
\r
  const handleSendTestWhatsApp = async () => {\r
    if (!testPhone) return alert(\"Please enter a target 10-digit mobile number.\");\r
    setIsSendingTestWa(true);\r
    try {\r
      await axios.post(`${API_BASE}/api/whatsapp/send`, {\r
        phone: testPhone,\r
        message: testMsg || 'Hello! 👋 This is a live test message sent from Cobb Store Automation Engine via local WhatsApp Gateway.'\r
      });\r
      alert(`✅ WhatsApp message sent successfully to ${testPhone}!`);\r
      setTestMsg('');\r
    } catch (err) {\r
      alert(`Failed to send WhatsApp message: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsSendingTestWa(false);\r
    }\r
  };\r
\r
  const handleSyncBroadcastGroup = async () => {\r
    setIsSyncingGroup(true);\r
    try {\r
      const res = await axios.post(`${API_BASE}/api/broadcast/sync`);\r
      alert(`✅ ${res.data.message}`);\r
      const groupRes = await axios.get(`${API_BASE}/api/broadcast/group`);\r
      setBroadcastGroup(groupRes.data.contacts || []);\r
      setBroadcastGroupCount(groupRes.data.totalCount || 0);\r
    } catch (err) {\r
      alert(`Sync failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsSyncingGroup(false);\r
    }\r
  };\r
\r
  const handleStartBroadcast = async () => {\r
    if (!broadcastMsg.trim()) return alert(\"Please enter a broadcast offer message.\");\r
    if (!isGatewayReady && !isGatewayRunning) {\r
      if (!confirm(\"WhatsApp Gateway seems offline. Proceeding will fail unless Gateway is started. Continue anyway?\")) return;\r
    }\r
    if (!confirm(`🚀 Are you sure you want to send this WhatsApp Offer Broadcast to ALL ${broadcastGroupCount} billed customers in your group?`)) return;\r
\r
    setIsStartingBroadcast(true);\r
    try {\r
      const res = await axios.post(`${API_BASE}/api/broadcast/start`, {\r
        message: broadcastMsg,\r
        delayMs: 1500\r
      });\r
      alert(`✅ ${res.data.message}`);\r
    } catch (err) {\r
      alert(`Broadcast failed to start: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsStartingBroadcast(false);\r
    }\r
  };\r
\r
  const handleStopBroadcast = async () => {\r
    try {\r
      await axios.post(`${API_BASE}/api/broadcast/stop`);\r
      alert(\"⏹️ Broadcast stop request sent.\");\r
    } catch (err) {\r
      alert(`Failed to stop broadcast: ${err.message}`);\r
    }\r
  };\r
\r
  const handleExportGroupCsv = () => {\r
    if (broadcastGroup.length === 0) return alert(\"Group is empty.\");\r
    let csv = \"Phone Number,Customer Name,Total Invoices,Total Spent (INR),Last Billed Date\
\";\r
    broadcastGroup.forEach(c => {\r
      csv += `\"${c.phone}\",\"${c.customerName || 'Valued Customer'}\",\"${c.totalBills || 1}\",\"${c.totalSpent || 0}\",\"${c.lastBilledAt || ''}\"\
`;\r
    });\r
    const blob = new Blob([csv], { type: 'text/csv' });\r
    const url = URL.createObjectURL(blob);\r
    const a = document.createElement('a');\r
    a.href = url;\r
    a.download = `cobb_billed_customer_group_${new Date().toISOString().slice(0, 10)}.csv`;\r
    a.click();\r
  };\r
\r
  const openCustomerCard = (customer) => {\r
    setSelectedCustomer(customer);\r
    setLoadingHistory(true);\r
    setGeneratedMsg('');\r
    setCustomerPersona('');\r
    \r
    axios.get(`${API_BASE}/api/customers/${customer.Phone}/history`)\r
      .then(res => {\r
        setCustomerHistory(res.data);\r
        if (res.data.length > 0) {\r
           setLoadingPersona(true);\r
           const recentItems = res.data.slice(0, 5).map(i => i.ArticleName).join(\", \");\r
           axios.post(`${API_BASE}/api/ai/persona`, { purchases: recentItems })\r
             .then(pRes => setCustomerPersona(pRes.data.persona))\r
             .catch(console.error)\r
             .finally(() => setLoadingPersona(false));\r
        }\r
      })\r
      .catch(console.error)\r
      .finally(() => setLoadingHistory(false));\r
  };\r
\r
  const handleGenerateAI = async () => {\r
    if (!selectedCustomer) return;\r
    setIsGenerating(true);\r
    const recentItems = customerHistory.slice(0, 3).map(i => i.ArticleName).join(\", \") || \"General Menswear\";\r
    const uniqueSizes = [...new Set(customerHistory.map(i => i.Size).filter(s => s && s !== 'Standard'))].join(\", \") || \"Standard\";\r
\r
    try {\r
      const res = await axios.post(`${API_BASE}/api/campaigns/generate`, {\r
        customerName: selectedCustomer.FirstName,\r
        pastPurchases: recentItems,\r
        type: aiMessageType,\r
        sizes: uniqueSizes\r
      });\r
      setGeneratedMsg(res.data.message);\r
    } catch (err) {\r
      alert(`AI Failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsGenerating(false);\r
    }\r
  };\r
\r
  const handleGenerateOutfitMatch = async (item) => {\r
    setActiveOutfitMatch(item.SKU);\r
    setIsGeneratingOutfit(true);\r
    try {\r
      const res = await axios.post(`${API_BASE}/api/ai/outfit-matcher`, {\r
        deadStockItem: item.ItemName\r
      });\r
      setOutfitPitch(res.data.message);\r
    } catch (err) {\r
      alert(`AI Failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsGeneratingOutfit(false);\r
    }\r
  };\r
\r
  const handleGenerateCampaign = async () => {\r
    setIsGeneratingCampaign(true);\r
    try {\r
      const res = await axios.post(`${API_BASE}/api/ai/campaign-builder`, {\r
        event: campaignEvent,\r
        audience: campaignAudience\r
      });\r
      setCampaignDraft(res.data.message);\r
    } catch (err) {\r
      alert(`AI Failed: ${err.response?.data?.error || err.message}`);\r
    } finally {\r
      setIsGeneratingCampaign(false);\r
    }\r
  };\r
\r
  const handleSaveReconciliation = async () => {\r
    const counted = parseFloat(countedCashInput) || 0;\r
    const system = overviewStats.today.CashAmount || 0;\r
    const variance = counted - system;\r
    \r
    try {\r
      const res = await axios.post(`${API_BASE}/api/reconciliation/save`, {\r
        systemCash: system,\r
        countedCash: counted,\r
        variance,\r
        notes: reconNotes,\r
        managerName: 'Store Manager'\r
      });\r
      setReconData(res.data.record);\r
      setShowReconModal(false);\r
      setCountedCashInput('');\r
      setReconNotes('');\r
    } catch (err) {\r
      alert(`Failed to save reconciliation: ${err.message}`);\r
    }\r
  };\r
\r
  const handleGenerateEodReport = async () => {\r
    try {\r
      const res = await axios.get(`${API_BASE}/api/reports/eod-summary`);\r
      setEodSummaryText(res.data.text);\r
      setShowEodModal(true);\r
    } catch (err) {\r
      alert(`Failed to generate report: ${err.message}`);\r
    }\r
  };\r
\r
  const handleMasterRestock = () => {\r
    const lowStockItems = inventory.filter(i => i.CurrentStock <= 3);\r
    if (lowStockItems.length === 0) {\r
      return alert(\"Great news! You have no low-stock items (< 3 units) right now.\");\r
    }\r
\r
    const grouped = lowStockItems.reduce((acc, curr) => {\r
      const type = curr.ProductType || 'Other';\r
      if (!acc[type]) acc[type] = [];\r
      acc[type].push(curr);\r
      return acc;\r
    }, {});\r
\r
    let message = `Hello Supplier, please process the following stock replenishment for *Cobb Pundri*:\
\
`;\r
    Object.entries(grouped).forEach(([type, items]) => {\r
      message += `*${type}*\
`;\r
      items.forEach(i => {\r
        message += `- [${i.ArticleNo}] ${i.ItemName} (${i.Color}, Size: ${i.Size}) - Only ${i.CurrentStock} left.\
`;\r
      });\r
      message += `\
`;\r
    });\r
\r
    message += `Please confirm availability and expected dispatch date.\
Regards,\
Parbhat Goyal`;\r
    window.open(`https://wa.me/?text=${encodeURIComponent(message)}`, '_blank');\r
  };\r
\r
  const formatCurrency = (amount) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(amount || 0);\r
\r
  const filteredInventory = inventory.filter(item => {\r
    const q = searchQuery.toLowerCase().trim();\r
    const cleanQ = q.replace(/^0+/, '');\r
    return (\r
      item.ArticleNo?.toLowerCase().includes(q) ||\r
      item.ItemName?.toLowerCase().includes(q) ||\r
      item.ProductType?.toLowerCase().includes(q) ||\r
      item.Color?.toLowerCase().includes(q) ||\r
      item.SKU?.toLowerCase().includes(q) ||\r
      (cleanQ.length > 2 && item.SKU?.toLowerCase().includes(cleanQ))\r
    );\r
  });\r
\r
  const groupedByType = filteredInventory.reduce((acc, item) => {\r
    const type = item.ProductType || 'Uncategorized';\r
    if (!acc[type]) acc[type] = [];\r
    acc[type].push(item);\r
    return acc;\r
  }, {});\r
\r
  const MASTER_CATEGORIES = [\r
    { id: 'Shirts', label: 'Shirts', icon: '👔' },\r
    { id: 'T-Shirts', label: 'T-Shirts & Polos', icon: '👕' },\r
    { id: 'Jeans', label: 'Jeans & Denims', icon: '👖' },\r
    { id: 'Formals', label: 'Formals & Trousers', icon: '🕴️' },\r
    { id: 'Accessories', label: 'Accessories (Belts, Wallets, Hanky, etc.)', icon: '⌚' },\r
    { id: 'Other Products', label: 'Other Products', icon: '🛍️' },\r
  ];\r
\r
  const navigationItems = [\r
    { category: \"Overview & P&L\", items: [\r
      { id: \"dashboard\", label: \"Dashboard\", icon: LayoutDashboard },\r
      { id: \"pnl\", label: \"Store P&L Statement\", icon: DollarSign, colorClass: \"text-green-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-green-500/15 text-green-400 font-bold border-l-2 border-green-500\" },\r
      { id: \"gst\", label: \"GST & Tax Summary\", icon: FileText, colorClass: \"text-emerald-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-emerald-500/15 text-emerald-400 font-bold border-l-2 border-emerald-500\" },\r
      { id: \"analytics\", label: \"Visual Rush Chart\", icon: Clock },\r
      { id: \"monthly\", label: \"Monthly Products\", icon: Calendar },\r
    ]},\r
    { category: \"Operations\", items: [\r
      { id: \"live\", label: \"Live Checkouts\", icon: Receipt },\r
      { id: \"topmovers\", label: \"Top Movers & Size Demand\", icon: Flame, colorClass: \"text-rose-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-rose-500/15 text-rose-400 font-bold border-l-2 border-rose-500\" },\r
      { id: \"inventory\", label: \"Live Inventory\", icon: Package },\r
      { id: \"sizematrix\", label: \"Size Matrix Heatmap\", icon: Grid, colorClass: \"text-blue-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-blue-500/15 text-blue-400 font-bold border-l-2 border-blue-500\" },\r
      { id: \"deadstock\", label: \"Dead Stock\", icon: Archive },\r
      { id: \"trending\", label: \"Trending Catalog\", icon: Star, colorClass: \"text-amber-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-amber-500/15 text-amber-400 font-bold border-l-2 border-amber-500\" },\r
    ]},\r
    { category: \"Marketing & CRM\", items: [\r
      { id: \"broadcast\", label: \"Mass Offer Broadcast\", icon: Send, colorClass: \"text-emerald-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-emerald-500/15 text-emerald-400 font-bold border-l-2 border-emerald-500\" },\r
      { id: \"wardrobe\", label: \"Wardrobe Profiler\", icon: Shirt, colorClass: \"text-purple-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-purple-500/15 text-purple-400 font-bold border-l-2 border-purple-500\" },\r
      { id: \"retention\", label: \"Retention Radar\", icon: Activity, colorClass: \"text-rose-400 hover:bg-slate-900 hover:text-white\", activeColorClass: \"bg-rose-500/15 text-rose-400 font-bold border-l-2 border-rose-500\" },\r
      { id: \"campaigns\", label: \"AI Campaigns\", icon: Megaphone, colorClass: \"text-indigo-400 hover:bg-slate-900 hover:text-indigo-300\", activeColorClass: \"bg-indigo-500/15 text-indigo-400 font-bold border-l-2 border-indigo-500\" },\r
      { id: \"vip\", label: \"VIP Profiles\", icon: Users },\r
      { id: \"dormant\", label: \"Dormant Clients\", icon: AlertCircle },\r
    ]},\r
    { category: \"System\", items: [\r
      { id: \"automation\", label: \"Automation Engine\", icon: Terminal },\r
    ]}\r
  ];\r
\r
  const classifySubCategory = (articleName = '', productType = '') => {\r
    const name = `${articleName} ${productType}`.toLowerCase();\r
    if (name.includes('jean') || name.includes('denim')) return 'Jeans';\r
    if (name.includes('t-shirt') || name.includes('tshirt') || name.includes('tee') || name.includes('polo') || name.includes('half sleeve')) return 'T-Shirts';\r
    if (name.includes('shirt')) return 'Shirts';\r
    if (name.includes('trouser') || name.includes('formal') || name.includes('suit') || name.includes('blazer') || name.includes('pant') || name.includes('chinos')) return 'Formals';\r
    if (name.includes('belt') || name.includes('wallet') || name.includes('tie') || name.includes('sock') || name.includes('perfume') || name.includes('deo') || name.includes('cap') || name.includes('hanky') || name.includes('handkerchief')) return 'Accessories';\r
    return 'Other Products';\r
  };\r
\r
  const groupedByMonth = monthlyProducts.reduce((acc, curr) => {\r
    const month = curr.SaleMonth || 'Unknown';\r
    const subCat = classifySubCategory(curr.ArticleName, curr.ProductType);\r
\r
    if (!acc[month]) {\r
      acc[month] = {\r
        data: { 'Shirts': [], 'T-Shirts': [], 'Jeans': [], 'Formals': [], 'Accessories': [], 'Other Products': [] },\r
        totalUnits: 0,\r
        totalRevenue: 0\r
      };\r
    }\r
\r
    acc[month].data[subCat].push(curr);\r
    acc[month].totalUnits += (curr.TotalUnitsSold || 0);\r
    acc[month].totalRevenue += (curr.TotalRevenue || 0);\r
    return acc;\r
  }, {});\r
\r
  const totalMonthlyUnits = monthlyProducts.reduce((acc, curr) => acc + (curr.TotalUnitsSold || 0), 0);\r
  const totalMonthlyRevenue = monthlyProducts.reduce((acc, curr) => acc + (curr.TotalRevenue || 0), 0);\r
  const maxHourlyRevenue = Math.max(...hourlySales.map(h => h.TotalRevenue), 1);\r
  const averageOrderValue = overviewStats.today.BillCount > 0 ? (overviewStats.today.TotalSales / overviewStats.today.BillCount) : 0;\r
  const targetProgress = Math.min((overviewStats.today.TotalSales / DAILY_TARGET) * 100, 100);\r
\r
  const handleGlobalSearch = (e) => {\r
    if (e.key === 'Enter' && searchQuery.trim() !== '') {\r
      const query = searchQuery.trim();\r
      if (/^[6-9]\\d{9}$/.test(query)) {\r
        setActiveTab('vip');\r
      } else {\r
        setActiveTab('inventory');\r
      }\r
      setToasts(prev => [\r
        ...prev, \r
        { id: Date.now(), title: 'STOCK SCAN / SEARCH 📷', message: `Filtering inventory for: ${query}` }\r
      ]);\r
    }\r
  };\r
\r
  const renderLogLine = (logText, idx) => {\r
    let textColor = \"text-slate-300\";\r
    let badgeColor = \"bg-slate-850 text-slate-400 border border-slate-800\";\r
    let badgeText = \"INFO\";\r
    \r
    const text = String(logText);\r
    \r
    if (text.includes(\"[SUCCESS]\")) {\r
      textColor = \"text-emerald-300\";\r
      badgeColor = \"bg-emerald-500/10 text-emerald-400 border border-emerald-500/20\";\r
      badgeText = \"SUCCESS\";\r
    } else if (text.includes(\"[FAILED]\") || text.includes(\"[ERROR]\") || text.includes(\"[FATAL]\")) {\r
      textColor = \"text-rose-300\";\r
      badgeColor = \"bg-rose-500/10 text-rose-400 border border-rose-500/20\";\r
      badgeText = \"ERROR\";\r
    } else if (text.includes(\"[SKIPPED]\")) {\r
      textColor = \"text-amber-300\";\r
      badgeColor = \"bg-amber-500/10 text-amber-400 border border-amber-500/20\";\r
      badgeText = \"SKIP\";\r
    } else if (text.includes(\"[NEW BILL DETECTED]\")) {\r
      textColor = \"text-cyan-300 font-semibold\";\r
      badgeColor = \"bg-cyan-500/10 text-cyan-400 border border-cyan-500/20\";\r
      badgeText = \"BILL\";\r
    }\r
\r
    const cleanText = text\r
      .replace(/\\[SUCCESS\\]|\\[FAILED\\]|\\[ERROR\\]|\\[FATAL\\]|\\[SKIPPED\\]|\\[NEW BILL DETECTED\\]/g, \"\")\r
      .trim();\r
\r
    return (\r
      <div key={idx} className={`py-1.5 px-3 rounded-lg hover:bg-slate-900/50 transition-colors flex items-start gap-3 border border-transparent hover:border-slate-850 font-mono text-[11px] ${textColor}`}>\r
        <span className={`inline-block px-1.5 py-0.5 rounded text-[8px] font-black tracking-wider uppercase ${badgeColor} flex-shrink-0`}>\r
          {badgeText}\r
        </span>\r
        <span className=\"flex-1 whitespace-pre-wrap\">{cleanText}</span>\r
      </div>\r
    );\r
  };\r
\r
  return (\r
    <div className={`flex h-screen font-sans transition-colors duration-300 ${darkMode ? 'dark-mode bg-slate-950 text-slate-100' : 'bg-slate-100 text-slate-800'}`}>\r
      <style>{`\r
        @keyframes slideIn {\r
          from {\r
            transform: translateY(1.5rem);\r
            opacity: 0;\r
          }\r
          to {\r
            transform: translateY(0);\r
            opacity: 1;\r
          }\r
        }\r
        .animate-slide-in {\r
          animation: slideIn 0.3s cubic-bezier(0.16, 1, 0.3, 1) forwards;\r
        }\r
        .custom-scrollbar::-webkit-scrollbar {\r
          width: 6px;\r
          height: 6px;\r
        }\r
        .custom-scrollbar::-webkit-scrollbar-track {\r
          background: transparent;\r
        }\r
        .custom-scrollbar::-webkit-scrollbar-thumb {\r
          background: #1e293b;\r
          border-radius: 9999px;\r
        }\r
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {\r
          background: #334155;\r
        }\r
\r
        /* Premium Custom Dark Mode Overrides */\r
        .dark-mode {\r
          background-color: #030712 !important;\r
          color: #f3f4f6 !important;\r
        }\r
        .dark-mode .bg-white {\r
          background-color: #0f172a !important;\r
          color: #f3f4f6 !important;\r
          border-color: #1e293b !important;\r
          box-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.4) !important;\r
        }\r
        .dark-mode .bg-slate-50 {\r
          background-color: #030712 !important;\r
          color: #e2e8f0 !important;\r
        }\r
        .dark-mode .bg-slate-100 {\r
          background-color: #030712 !important;\r
        }\r
        .dark-mode .bg-slate-50\\/50 {\r
          background-color: rgba(15, 23, 42, 0.4) !important;\r
        }\r
        .dark-mode .bg-white\\/85, .dark-mode .bg-white\\/80 {\r
          background-color: rgba(3, 7, 18, 0.8) !important;\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .text-slate-800 {\r
          color: #f9fafb !important;\r
        }\r
        .dark-mode .text-slate-700 {\r
          color: #e5e7eb !important;\r
        }\r
        .dark-mode .text-slate-650, .dark-mode .text-slate-600 {\r
          color: #cbd5e1 !important;\r
        }\r
        .dark-mode .text-slate-500 {\r
          color: #94a3b8 !important;\r
        }\r
        .dark-mode .text-slate-400 {\r
          color: #64748b !important;\r
        }\r
        .dark-mode .border-slate-200 {\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .border-slate-100 {\r
          border-color: #0f172a !important;\r
        }\r
        .dark-mode .divide-slate-100 > * + * {\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .divide-slate-100 {\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode input, .dark-mode select, .dark-mode textarea {\r
          background-color: #0f172a !important;\r
          border-color: #334155 !important;\r
          color: #f8fafc !important;\r
        }\r
        .dark-mode input:focus, .dark-mode select:focus, .dark-mode textarea:focus {\r
          border-color: #3b82f6 !important;\r
          box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.25) !important;\r
        }\r
        .dark-mode input::placeholder {\r
          color: #475569 !important;\r
        }\r
        .dark-mode table th {\r
          background-color: #090d16 !important;\r
          color: #cbd5e1 !important;\r
          border-bottom: 1px solid #1e293b !important;\r
        }\r
        .dark-mode table tr {\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode table tr:hover {\r
          background-color: rgba(30, 41, 59, 0.4) !important;\r
        }\r
        .dark-mode .hover\\/bg-slate-50:hover {\r
          background-color: #1e293b !important;\r
        }\r
        .dark-mode .hover\\/bg-blue-50\\/30:hover {\r
          background-color: rgba(30, 41, 59, 0.2) !important;\r
        }\r
        .dark-mode .bg-blue-50 {\r
          background-color: rgba(30, 41, 59, 0.6) !important;\r
          color: #60a5fa !important;\r
          border-color: rgba(96, 165, 250, 0.2) !important;\r
        }\r
        .dark-mode .text-blue-600 {\r
          color: #60a5fa !important;\r
        }\r
        .dark-mode .text-blue-800 {\r
          color: #93c5fd !important;\r
        }\r
        .dark-mode .bg-green-50 {\r
          background-color: rgba(6, 78, 59, 0.4) !important;\r
          color: #34d399 !important;\r
          border-color: rgba(52, 211, 153, 0.2) !important;\r
        }\r
        .dark-mode .text-green-600 {\r
          color: #34d399 !important;\r
        }\r
        .dark-mode .bg-red-50 {\r
          background-color: rgba(127, 29, 29, 0.4) !important;\r
          color: #f87171 !important;\r
          border-color: rgba(248, 113, 113, 0.2) !important;\r
        }\r
        .dark-mode .text-red-500 {\r
          color: #f87171 !important;\r
        }\r
        .dark-mode .text-red-900 {\r
          color: #fca5a5 !important;\r
        }\r
        .dark-mode .bg-red-50\\/50 {\r
          background-color: rgba(239, 68, 68, 0.03) !important;\r
        }\r
        .dark-mode .border-red-100 {\r
          border-color: rgba(239, 68, 68, 0.15) !important;\r
        }\r
        .dark-mode .bg-red-100 {\r
          background-color: rgba(239, 68, 68, 0.2) !important;\r
          color: #f87171 !important;\r
        }\r
        .dark-mode .bg-amber-50 {\r
          background-color: rgba(120, 53, 4, 0.4) !important;\r
          color: #fbbf24 !important;\r
          border-color: rgba(251, 191, 36, 0.2) !important;\r
        }\r
        .dark-mode .text-amber-600 {\r
          color: #fbbf24 !important;\r
        }\r
        .dark-mode .text-amber-700 {\r
          color: #fde047 !important;\r
        }\r
        .dark-mode .bg-indigo-50 {\r
          background-color: rgba(49, 46, 129, 0.4) !important;\r
          color: #818cf8 !important;\r
          border-color: rgba(129, 140, 248, 0.2) !important;\r
        }\r
        .dark-mode .text-indigo-600 {\r
          color: #818cf8 !important;\r
        }\r
        .dark-mode .hover\\:bg-indigo-50:hover {\r
          background-color: rgba(99, 102, 241, 0.15) !important;\r
        }\r
        .dark-mode .border-indigo-200 {\r
          border-color: rgba(99, 102, 241, 0.3) !important;\r
        }\r
        .dark-mode .bg-slate-50\\/80 {\r
          background-color: rgba(15, 23, 42, 0.8) !important;\r
        }\r
        .dark-mode .bg-indigo-100 {\r
          background-color: rgba(99, 102, 241, 0.15) !important;\r
          color: #a5b4fc !important;\r
          border-color: rgba(165, 180, 252, 0.2) !important;\r
        }\r
        .dark-mode .text-indigo-700 {\r
          color: #c7d2fe !important;\r
        }\r
        .dark-mode .bg-indigo-950, .dark-mode .bg-indigo-900 {\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .from-slate-900, .dark-mode .to-indigo-950 {\r
          background-image: none !important;\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .bg-slate-900 {\r
          background-color: #090d16 !important;\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .bg-slate-950 {\r
          background-color: #020617 !important;\r
        }\r
        .dark-mode .bg-slate-200 {\r
          background-color: #1e293b !important;\r
        }\r
        .dark-mode .bg-gradient-to-t.from-blue-600 {\r
          background-image: linear-gradient(to top, #2563eb, #60a5fa) !important;\r
        }\r
        .dark-mode .bg-white.w-full.max-w-xl {\r
          background-color: #090d16 !important;\r
          border-left: 1px solid #1e293b !important;\r
        }\r
        .dark-mode .bg-slate-50.p-8,\r
        .dark-mode .bg-slate-50.p-6,\r
        .dark-mode .bg-slate-50.border-b {\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
        }\r
        .dark-mode .dark-toggle-btn {\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
          color: #fbbf24 !important;\r
        }\r
        .dark-mode .dark-toggle-btn:hover {\r
          background-color: #1e293b !important;\r
        }\r
        .dark-mode .status-badge {\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
          color: #e2e8f0 !important;\r
        }\r
        .dark-mode .from-indigo-900 {\r
          background-image: none !important;\r
          background-color: #0f172a !important;\r
          border-color: #1e293b !important;\r
        }\r
      `}</style>\r
      \r
      {/* Mobile Drawer Overlay */}\r
      {isMobileMenuOpen && (\r
        <div \r
          onClick={() => setIsMobileMenuOpen(false)}\r
          className=\"fixed inset-0 bg-slate-950/70 backdrop-blur-sm z-30 lg:hidden\"\r
        />\r
      )}\r
\r
      {/* Sidebar Navigation */}\r
      <div className={`fixed lg:static inset-y-0 left-0 w-64 bg-slate-950 text-slate-300 flex flex-col shadow-xl z-40 border-r border-slate-800 transition-transform duration-300 ease-in-out ${isMobileMenuOpen ? 'translate-x-0' : '-translate-x-full lg:translate-x-0'}`}>\r
        <div className=\"p-6 border-b border-slate-800 flex justify-between items-center\">\r
          <div>\r
            <div className=\"flex items-center gap-3 mb-1\">\r
              <div className=\"w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center shadow-lg shadow-blue-500/30\">\r
                <span className=\"text-white font-bold text-lg\">C</span>\r
              </div>\r
              <h1 className=\"text-xl font-bold tracking-wider text-white\">COBB ITALY</h1>\r
            </div>\r
            <p className=\"text-slate-500 text-xs font-medium ml-11\">Smart CRM System</p>\r
          </div>\r
          <button \r
            onClick={() => setIsMobileMenuOpen(false)}\r
            className=\"lg:hidden text-slate-400 hover:text-white p-1 rounded-lg hover:bg-slate-900\"\r
          >\r
            <X className=\"w-5 h-5\" />\r
          </button>\r
        </div>\r
        \r
        <nav className=\"flex-1 px-3 space-y-4 mt-6 overflow-y-auto custom-scrollbar\">\r
          {navigationItems.map((cat, catIdx) => (\r
            <div key={catIdx} className=\"space-y-1\">\r
              <p className=\"px-4 text-[9px] font-bold text-slate-600 uppercase tracking-widest mb-1.5\">{cat.category}</p>\r
              {cat.items.map((item) => {\r
                const Icon = item.icon;\r
                const isActive = activeTab === item.id;\r
                \r
                const normalColor = item.colorClass || \"text-slate-400 hover:bg-slate-900/60 hover:text-slate-200\";\r
                const activeColor = item.activeColorClass || \"bg-blue-600/15 text-blue-400 font-semibold border-l-2 border-blue-500\";\r
\r
                return (\r
                  <button \r
                    key={item.id}\r
                    onClick={() => { setActiveTab(item.id); setSearchQuery(''); setIsMobileMenuOpen(false); }} \r
                    className={`w-
<truncated 15856 bytes>

NOTE: The output was truncated because it was too long. Use a more targeted query or a smaller range to get the information you need."}

{"step_index":130,"source":"MODEL","type":"PLANNER_RESPONSE","status":"DONE","created_at":"2026-08-27T09:04:52Z","thinking":"**Considering Toolset Options**

I'm currently evaluating the available tools, recognizing some are task-specific, like 'view_file', while others offer broader functionality for command execution. I need to consider how to best leverage these tools in combination to achieve the desired outcome. The focus is on finding the most efficient approach.


**Prioritizing Tool Specificity**

I'm focusing now on adhering to specific tool selection rules: avoid `cat` for file creation/appending, favor `grep_search` over `grep` in bash, and refrain from using general tools (`ls`, `cat`, `grep`, `sed`) when specialized options exist. Before execution, I'll explicitly list all pertinent tools, ensuring the chosen set is the most precise for the task at hand.


**Analyzing Diff Anomalies**

I'm now investigating a discrepancy in how changes were applied across file chunks. My focus is on understanding the displacement of code lines during a multi-replace operation. I'm examining line number discrepancies within `view_file` outputs. Specifically, I'm trying to figure out where the `toggleListener` function ended up. I am focusing on the `multi_replace_file_content` block. I need to get a clear picture of how lines 411-436 got displaced in the file.


**Resolving File Modification Issues**

I've pinpointed an error: the incorrect chunk ranges in `multi_replace_file_content` led to unintended merging and line displacement, specifically the `toggleListener` function's movement. It looks like the issue stems from an overlap. I mistakenly thought there was a mismatch, but I now see the `TargetContent` in Chunk 6 *did* match perfectly. This clarified the problem, which I am now correcting by adjusting the chunk start and end lines.

